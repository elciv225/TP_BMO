<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<!-- Interface de réunion avec style WhatsApp -->
<VBox fx:id="reunionContainer" styleClass="reunion-container" maxHeight="-Infinity" maxWidth="-Infinity" minHeight="-Infinity" minWidth="-Infinity" prefHeight="600.0" prefWidth="800.0" stylesheets="@styles/main.css" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="client.ReunionController">
    <children>
        <!-- En-tête de la réunion avec statut de connexion -->
        <Label fx:id="connectionStatus" styleClass="connectionStatus, connected" text="Connecté à la réunion" prefHeight="50.0" maxWidth="Infinity">
            <padding>
                <Insets top="15.0" bottom="15.0" left="20.0" right="20.0"/>
            </padding>
        </Label>

        <!-- Zone de messages principale avec scroll -->
        <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" styleClass="scroll-pane" prefHeight="400.0">
            <content>
                <!-- Padding is handled by .scroll-pane .content in CSS -->
                <VBox fx:id="messageArea" styleClass="messageArea" spacing="8.0" />
            </content>
        </ScrollPane>

        <!-- Zone d'invitation (visible uniquement pour l'organisateur) -->
        <HBox fx:id="invitationArea" alignment="CENTER_LEFT" spacing="15.0" styleClass="invitation-area" managed="false" visible="false" prefHeight="60.0">
            <padding>
                <Insets top="12.0" bottom="12.0" left="20.0" right="20.0"/>
            </padding>
            <children>
                <Label text="👥 Inviter un participant:" styleClass="field-label" minWidth="-Infinity"/>
                <TextField fx:id="inviteUserField" styleClass="styled-text-field, invite-user-field" prefHeight="35.0" prefWidth="250.0" promptText="Nom d'utilisateur à inviter" />
                <Button fx:id="inviteButton" styleClass="invite-button" mnemonicParsing="false" text="Inviter" prefHeight="35.0" minWidth="80.0"/>
            </children>
        </HBox>

        <!-- Zone de saisie de message style WhatsApp -->
        <HBox fx:id="inputArea" styleClass="inputArea" alignment="CENTER_LEFT" spacing="12.0" prefHeight="70.0" maxHeight="70.0" minHeight="70.0">
            <padding>
                <Insets top="15.0" bottom="15.0" left="20.0" right="20.0"/>
            </padding>
            <children>
                <!-- Champ de saisie du message -->
                <TextField fx:id="messageInput" styleClass="styled-text-field" prefHeight="40.0" HBox.hgrow="ALWAYS" promptText="Tapez votre message ici..." minWidth="200.0"/>

                <!-- Bouton d'envoi -->
                <Button fx:id="sendButton" styleClass="button-base" mnemonicParsing="false" text="📤" prefWidth="50.0" prefHeight="40.0" minWidth="50.0" maxWidth="50.0"/>
            </children>
        </HBox>
    </children>
</VBox>